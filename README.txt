Application is build on Node.js

Download and install Node.js and npm.
Navigate to this directory from command prompt.
type "npm install" it should all the needed components for the backend.
then  type "bower install"  it will install components for the front end.

Finally run the Node app to start the application.

Currently, site and database is hosted with firebase.com.
